package main

import (
	"fmt"
)

/*
แกรมนี้แสดงให้เห็นถึงความสำคัญของการตรวจสอบว่าพอยน์เตอร์ไม่ใช่ nil ก่อนที่จะพยายามเข้าถึงค่าผ่านมัน เพื่อหลีกเลี่ยงข้อผิดพลาดในรันไทม์ที่อาจเกิดขึ้น
*/

func main() {
    var a *float64  // ประกาศตัวแปรพอยน์เตอร์ a สำหรับประเภท float64 โดยไม่ได้กำหนดที่อยู่
    fmt.Println(*a) // พยายามเข้าถึงค่าของพอยน์เตอร์ a ซึ่งเป็น nil จะทำให้เกิด panic
}
/* 

*/